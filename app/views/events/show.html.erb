<p id="notice"><%= notice %></p>

<p>
  <strong>姓名:</strong>
  <%= @event.title %>
</p>

<p>
  <strong>開始時間:</strong>
  <%= @event.start_at %>
</p>

<p>
  <strong>結束時間:</strong>
  <%= @event.end_at %>
</p>

<%= link_to '編輯', edit_event_path(@event) %> |
<%= link_to '返回', events_path %>
<%= link_to("刪除", event_path, :method => :delete) %>
